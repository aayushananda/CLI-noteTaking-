#!/usr/bin/env node 
//The line above is called a hashbang 
// A hashbang is a special command placed on top of a script that tells the OS which lang to use when running the script
// In the context of this project the hanshbang tells the machine to use NodeJs as an interpreter to execute this js file
// It has to be the first thing in the file to work
console.log("hello world!")

